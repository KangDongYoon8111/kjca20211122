using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OhSeonghyeon20211228 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string a = "사각형의 너비는 30 입니다.";
        string b = "사각형의 높이는 40 입니다.";
        string c = "사각형의 넓이 : 1200";

        Debug.Log(a); 
        Debug.Log(b);
        Debug.Log(c);
     
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
