using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OhsangMok_EX2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int a = 49;

        if (a > 50)
        {
            if (a > 50)
                Debug.Log("50이상");
        }
        else
        { Debug.Log("50이하"); };
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
