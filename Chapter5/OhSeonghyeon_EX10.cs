using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OhSeonghyeon_EX10 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string a = "¡Ú";
        string b = "¡Ù";

        int x = 1;
        int y = 2;

        for (x = 1; x <= 1; x++) 
        {
            if (x <= 1)
            { Debug.Log($"{a}{b}{b}{b}{b}"); }
        }
        for (x = 2; x <= 2; x++)
        {
            if (x <= 2)
            { Debug.Log($"{a}{a}{b}{b}{b}"); }
        }
        for (x = 3; x <= 3; x++)
        {
            if (x <= 3)
            { Debug.Log($"{a}{a}{a}{b}{b}"); }

        }
        for (x = 4; x <= 4; x++)
        {
             
            if (x <= 4)
            { Debug.Log($"{a}{a}{a}{a}{b}"); }


        }
        for (x = 5; x <= 5; x++)
        {
            if (x <= 5)
            { Debug.Log($"{a}{a}{a}{a}{a}"); }
        }
        







        
            

        
        
        








    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
