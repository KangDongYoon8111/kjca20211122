        1.i++와++i의 차이점은 무엇인가요 ?
            i++ 는 증가연산이고
            ++i는 증가연산자를 먼저 계산하고 그값을 대입한다

      2.각 출력문의 값을 적으세요
int i = 1;
        Debug.Log(i = i + 1); 답 = "i+2";
        Debug.Log(i++); 답:3
        Debug.Log(++i); 답:1
        Debug.Log(i += 1); 답:2

        3.다음 코드에서 b는 어떤 값을 가질까요 ?
       int a = 10;
        string b = a == 0 ? "가나다" : "ABC";
        거짓
